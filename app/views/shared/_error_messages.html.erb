<% if @a.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-error">
      <%= pluralize(@a.errors.count, "error") %> prohibited this a from being saved:
    </div>
    <ul>
    <% @a.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>